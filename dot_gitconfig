[user]
	email = git@lewis.wales
	name = lew
[filter "lfs"]
	required = true
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
[submodule]
	recurse = true
[push]
	recurseSubmodules = on-demand
